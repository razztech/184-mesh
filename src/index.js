import "./styles.css";
import WebGLFont from "./components/WebGLFont/WebGLFont";

const type = new WebGLFont({
  word: "OCEAN",
  position: [-80, -10, 0],
  rotation: [Math.PI, 0, 0],
  zoom: 100
});
